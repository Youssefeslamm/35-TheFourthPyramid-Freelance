<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="proposal-service"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
        <http>
            <url>http://loki.monitoring.svc.cluster.local:3100/loki/api/v1/push</url>
        </http>
        <format>
            <label>
                <pattern>app=freelance,service=${APP_NAME},level=%level,env=k8s</pattern>
            </label>
            <message>
                <pattern><![CDATA[{ "timestamp": "%d{ISO8601}", "level": "%level", "service": "${APP_NAME}", "thread": "%thread", "logger": "%logger{36}", "correlationId": "%X{correlationId:-}", "userId": "%X{userId:-}", "jobId": "%X{jobId:-}", "proposalId": "%X{proposalId:-}", "contractId": "%X{contractId:-}", "payoutId": "%X{payoutId:-}", "routingKey": "%X{routingKey:-}", "message": "%msg" }]]></pattern>
            </message>
        </format>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.neo4j" level="WARN"/>
    <logger name="com.team35.freelance.proposal" level="INFO"/>

</configuration>
