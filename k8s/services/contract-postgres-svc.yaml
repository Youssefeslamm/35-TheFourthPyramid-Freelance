apiVersion: v1
kind: Service
metadata:
  name: contract-postgres
  namespace: freelance
spec:
  clusterIP: None
  selector:
    app: contract-postgres
  ports:
    - port: 5432
      targetPort: 5432
