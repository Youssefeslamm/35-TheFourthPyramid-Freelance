apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  labels:
    app: grafana
    grafana_dashboard: "1"
data:
  milestone3-overview.json: |
    {
      "title": "Freelance Marketplace Milestone 3",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "panels": [
        {
          "type": "timeseries",
          "title": "HTTP Requests Placeholder",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count[5m])) by (application)"
            }
          ],
          "gridPos": {
            "x": 0,
            "y": 0,
            "w": 12,
            "h": 8
          }
        }
      ]
    }
  proposal-dashboard.json: |
    {
      "title": "Proposal Service Dashboard",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "panels": [
        {
          "type": "timeseries",
          "title": "Error Rate - Proposal Service",
          "description": "Count of ERROR-level log lines per minute",
          "targets": [
            {
              "expr": "sum(count_over_time({service=\"proposal-service\", level=\"ERROR\"}[1m]))",
              "legendFormat": "Errors/min"
            }
          ],
          "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "RabbitMQ Event Audit - Proposal Service",
          "description": "Events published vs consumed by proposal-service",
          "targets": [
            {
              "expr": "sum(count_over_time({service=\"proposal-service\"} |= \"Published\" [5m]))",
              "legendFormat": "Published"
            },
            {
              "expr": "sum(count_over_time({service=\"proposal-service\"} |= \"Consuming\" [5m]))",
              "legendFormat": "Consumed"
            }
          ],
          "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "Saga State Transitions - Proposal Service",
          "description": "Saga state transitions for proposals",
          "targets": [
            {
              "expr": "sum(count_over_time({service=\"proposal-service\"} |= \"transitioning\" [5m]))",
              "legendFormat": "Transitions/5m"
            }
          ],
          "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "HTTP Request Rate - Proposal Service",
          "description": "Requests per second per endpoint",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count{job=\"proposal-service\"}[5m])) by (uri)",
              "legendFormat": "{{uri}}"
            }
          ],
          "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "HTTP Latency P99 - Proposal Service",
          "description": "P99 latency per endpoint",
          "targets": [
            {
              "expr": "histogram_quantile(0.99, sum(rate(http_server_requests_seconds_bucket{job=\"proposal-service\"}[5m])) by (le, uri))",
              "legendFormat": "P99 {{uri}}"
            }
          ],
          "gridPos": { "x": 0, "y": 16, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "JVM Heap Usage - Proposal Service",
          "description": "JVM heap memory usage",
          "targets": [
            {
              "expr": "jvm_memory_used_bytes{job=\"proposal-service\", area=\"heap\"}",
              "legendFormat": "Heap Used"
            }
          ],
          "gridPos": { "x": 12, "y": 16, "w": 12, "h": 8 }
        }
      ]
    }
  user-service-dashboard.json: |
    {
      "title": "User Service Dashboard",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "panels": [
        {
          "type": "timeseries",
          "title": "HTTP Request Rate - User Service",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count{job=\"user-service\"}[5m])) by (uri)",
              "legendFormat": "{{uri}}"
            }
          ],
          "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "RabbitMQ Listener Rate - User Service",
          "targets": [
            {
              "expr": "sum(rate(spring_rabbitmq_listener_seconds_count{job=\"user-service\"}[5m])) by (listener)",
              "legendFormat": "{{listener}}"
            }
          ],
          "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "JVM Heap Usage - User Service",
          "targets": [
            {
              "expr": "jvm_memory_used_bytes{job=\"user-service\", area=\"heap\"}",
              "legendFormat": "Heap Used"
            }
          ],
          "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 }
        }
      ]
    }
  wallet-service-dashboard.json: |
    {
      "title": "Wallet Service Dashboard",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "30s",
      "panels": [
        {
          "type": "timeseries",
          "title": "HTTP Request Rate - Wallet Service",
          "description": "Requests per second by endpoint and method",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count{job=\"wallet-service\"}[5m])) by (uri, method)",
              "legendFormat": "{{method}} {{uri}}"
            }
          ],
          "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "HTTP Error Rate - Wallet Service",
          "description": "4xx and 5xx responses per second by status and endpoint",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_count{job=\"wallet-service\", status=~\"4..|5..\"}[5m])) by (status, uri)",
              "legendFormat": "{{status}} {{uri}}"
            }
          ],
          "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "HTTP Latency P95 - Wallet Service",
          "description": "P95 request latency by endpoint",
          "targets": [
            {
              "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket{job=\"wallet-service\"}[5m])) by (le, uri))",
              "legendFormat": "P95 {{uri}}"
            }
          ],
          "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "Average HTTP Latency - Wallet Service",
          "description": "Average request duration by endpoint",
          "targets": [
            {
              "expr": "sum(rate(http_server_requests_seconds_sum{job=\"wallet-service\"}[5m])) by (uri) / sum(rate(http_server_requests_seconds_count{job=\"wallet-service\"}[5m])) by (uri)",
              "legendFormat": "{{uri}}"
            }
          ],
          "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 }
        },
        {
          "type": "timeseries",
          "title": "JVM Heap Usage - Wallet Service",
          "description": "Heap memory currently used",
          "targets": [
            {
              "expr": "jvm_memory_used_bytes{job=\"wallet-service\", area=\"heap\"}",
              "legendFormat": "Heap Used"
            }
          ],
          "gridPos": { "x": 0, "y": 16, "w": 12, "h": 8 }
        }
      ]
    }
