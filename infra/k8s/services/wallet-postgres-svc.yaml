apiVersion: v1
kind: Service
metadata:
  name: wallet-postgres
  namespace: freelance
  labels:
    app: wallet-postgres
spec:
  clusterIP: None
  selector:
    app: wallet-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
