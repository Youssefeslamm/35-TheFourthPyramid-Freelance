apiVersion: 1

providers:
  - name: wallet-dashboards
    orgId: 1
    folder: Wallet
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
